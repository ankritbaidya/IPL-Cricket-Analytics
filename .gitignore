# Raw data (large; download instructions are in data/README.md)
data/*.csv

# Pipeline outputs (regenerated by running the script — don't commit generated
# artifacts; commit the code that makes them instead)
outputs/

# Python
__pycache__/
*.pyc
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
